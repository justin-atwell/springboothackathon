service: default

runtime: java
env: flex

runtime_config:
  jdk: openjdk8

handlers:
  - url: /.*
    script: this field is required, but ignored

automatic-scaling:
  max-instances: 2

health_check:
  enable_health_check: False
